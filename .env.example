# Cadence — Supabase project (post-migration)
# Get these from the Supabase dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Legacy custom Express+Prisma backend on Railway. Still required while the
# frontend is mid-migration. Will be removed at the Supabase cutover.
VITE_API_URL=http://localhost:3000
